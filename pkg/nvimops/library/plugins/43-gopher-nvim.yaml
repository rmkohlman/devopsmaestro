apiVersion: devopsmaestro.io/v1
kind: NvimPlugin
metadata:
  name: gopher-nvim
  description: "Go development tools - struct tags, interface impl, test generation"
  category: language
  tags: ["golang", "tools", "codegen"]
spec:
  repo: olexsmir/gopher.nvim
  dependencies:
    - repo: nvim-lua/plenary.nvim
  config: |
    require("gopher").setup()
