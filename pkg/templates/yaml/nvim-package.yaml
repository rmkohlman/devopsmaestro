# nvim-package — Collection of nvim plugins
# Groups plugins together for reuse across workspaces
apiVersion: devopsmaestro.io/v1
kind: NvimPackage
metadata:
  name: my-nvim-package
  description: ""
  category: ""
  labels: {}
spec:
  extends: ""
  plugins:
    - plugin-name
