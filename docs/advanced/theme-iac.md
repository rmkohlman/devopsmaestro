# Theme Infrastructure as Code

!!! info "Moved to MaestroTheme"
    Theme IaC documentation has moved to the dedicated MaestroTheme module site.

    **[MaestroTheme Documentation →](https://rmkohlman.github.io/MaestroTheme/)**

The `dvm` CLI supports applying any resource YAML file — including `NvimTheme` resources — using the standard `dvm apply -f` command. For full theme YAML reference and IaC workflows, see the MaestroTheme docs above.
