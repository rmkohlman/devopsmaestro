# ADR-008: Shared Library Architecture for Neovim Management

**Status:** Superseded  
**Date:** 2026-01-23  
**Superseded by:** See `decisions.md` ADR-008 for the implemented design.

`nvp` was built as a standalone binary within the same repository (`cmd/nvp/`), not as a separate shared library. The shared library approach described in the original ADR was never implemented.
